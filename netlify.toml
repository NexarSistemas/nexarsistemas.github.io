[functions]
  directory = "netlify/functions"

[build]
  ignore = """
    if [ -z "$CACHED_COMMIT_REF" ]; then
      exit 1
    fi

    if git diff --quiet "$CACHED_COMMIT_REF" "$COMMIT_REF" -- netlify/functions netlify/lib netlify.toml _redirects; then
      exit 0
    fi

    exit 1
  """
